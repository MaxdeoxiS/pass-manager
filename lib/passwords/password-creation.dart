import 'package:flutter/material.dart';

class PasswordCreation extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Création de mdp"),
      ),
      body: Text("PasswordCreation"),
    );
  }
}